class Todo {
  // in TS you have to define the properties of this class ahead of time
  id: string;
  text: string;

  constructor(todoText: string) {
    this.text = todoText;
    this.id = new Date().toISOString();
  }
}

export default Todo;
